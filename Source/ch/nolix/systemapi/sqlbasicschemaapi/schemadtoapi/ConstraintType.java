//package declaration
package ch.nolix.systemapi.sqlbasicschemaapi.schemadtoapi;

//enum
public enum ConstraintType {
    NOT_NULL,
    UNIQUE,
    CHECK,
    DEFAULT,
    PRIMARY_KEY,
    FOREIGN_KEY,
}
